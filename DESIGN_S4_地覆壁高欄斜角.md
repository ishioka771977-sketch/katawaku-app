# S4: 地覆・壁高欄の斜め端部 — 設計書兼実装記録

作成: 2026-07-07 / 状態: 実装・検証済み / くろたん論点3つへの回答を含む

## 論点1への回答: 「端部だけ」の切り分け

**斜角パラメータで持つのは端部の幾何のみ。本体は直橋と完全同一。**

- スキーマ: `structure.skew_angle_deg` ＋ `skew_direction`（deck_slabと同語彙・トップレベル）
- 直橋と同一のまま: 断面・延長 `length_mm`（**車道側面基準**と定義）・パネル割付・セパ・リフト
- 斜角で変わるもの（アプリが検証カードで自動計算）:
  - 妻面幅 = 各部材幅 ÷ sinθ（地覆底版577/地覆404/壁高欄基部346 @60°例）
  - 側面長差 = 各部材幅 ÷ tanθ（車道側と外側の延長差）
  - 端部パネルの平面カット角 = 90°−θ
- deck_slabのような全体平行四辺形化は**しない**（線状構造物で本体が不変のため）

## 論点2への回答: AB位置の基点

**`anchor_bolts.positions[].x_mm` は従来通り「A1端の橋軸直交面からの追い距離（直角投影）」を維持。**
理由: 図面の追い距離は橋軸沿いが通例で、直橋と同じ測り方＝既存JSONと互換。斜め妻面起点だと側面ごとに基点が変わり事故る。
端部干渉は「**AB端部余裕（最小）= min(先頭x, L−末尾x) − 幅÷tanθ**」で評価。負値＝斜め端面からのはみ出し＝⚠（アプリが自動判定）。

## 論点3への回答: 目地一致の測り方

**直角投影（橋軸沿いの追い距離）で一致を測る。** 床版の伸縮目地も橋軸沿い定義なので、C1 の `joint_alignment` と自然に整合。斜め端部起点は採らない。

## 実装（parapet.js validateSkewEnd）

- overview 冒頭に「斜め端部検証」カード（斜角時のみ表示）: 妻面幅×3・側面長差×2・AB端部余裕
- Node検証: 直橋=非表示／60°正常=✓6整合OK／ABはみ出し=⚠検出。数値は全て手計算一致
- **3Dは端部の斜め未表現**（本体不変のため直橋表示のまま。寸法・数量・検証は対応）— md v7 に明記

## 残（対象外）

- 3Dでの端面斜め表示（需要が出たら。エンジンは対応済みなので追加は小）
- 左右不等斜
