# カタチ 斜角（斜橋）対応 — 設計書

> 現場優先度 第1位（くろたん査定: 「斜橋を引いた瞬間に型知が使えなくなる」）。
> 斜角があると床版端部・地覆・壁高欄の端部パネルが全て斜めカットになり、目地位置も数量も変わる。

作成: 2026-07-04 / 状態: **設計レビュー待ち（実装未着手）** / 前提: 3Dエンジン四隅化済み

---

## 0. 対応の全体構造（5層）

型知は「数量・割付=くろたんがJSONに書く／アプリ=表示・検証」の分業なので、斜角対応は次の5層に分かれる。
**アプリのコードだけでは完結しない**（層3のmd整備が同格に重要）。

| 層 | 内容 | 担当 |
|---|---|---|
| 1. スキーマ | `skew_angle_deg` の定義と置き場所 | 本設計 |
| 2. くろたん計算ルール | 端部カットパネルの割付・寸法・数量の計算法（md掲載） | 本設計＋くろたん監修 |
| 3. 2D描画 | 平面図の平行四辺形化・端部カット可視化 | アプリ |
| 4. 3D描画 | 床版輪郭・妻型枠の斜め配置 | アプリ |
| 5. 検証 | 妻面長さ・パネル幅合計の斜角整合チェック | アプリ |

## 1. スキーマ設計

```json
"dimensions": {
  "width_mm": 20500,          // 幅員（道路直角方向で測る・従来通り）
  "length_mm": 33000,         // 桁長（橋軸方向・従来通り）
  "skew_angle_deg": 60        // 斜角θ: 支承線と橋軸のなす角。90=直橋（省略時90）
}
```

- **斜角θの定義は道路橋通例に従う**: 支承線と橋軸のなす角。90°=直橋。60°なら30°振れた斜橋。
- 置き場所は `dimensions` 直下。deck_slab から導入し、parapet_curb_and_barrier に横展開。
- **左右不等斜（A1とA2で斜角が違う）は当面対象外**（描けない形として申告継続）。必要になったら `skew_angle_a1_deg`/`skew_angle_a2_deg` に分離できる後方互換設計。

### 導かれる幾何（アプリ・くろたん共通の計算基準）

- 妻面（A/A'）の実長: `width_mm / sin(θ)` （60°なら 20,500/sin60° ≈ 23,672mm）
- 平面の食い違い量: `width_mm / tan(θ)` （60°なら ≈ 11,836mm。A1側基準でA2側がずれる方向は `skew_direction`: `"left"`/`"right"` で指定、既定 `"right"`）
- 端部カットパネルの斜めカット角: `90° - θ`（60°斜橋なら30°カット）

## 2. くろたん計算ルール（md v5 掲載案の骨子）

- パネルは従来通り**橋軸直角方向に流す**（現場通例・資材効率優先）。
- **端部（A/A'寄り）の各段で、支承線にかかるパネルを斜めカット**。カット寸法は
  `カット長 = パネル位置のずれ量 = (そのパネルの橋軸方向位置) / tan(θ)` で各段一意に決まる。
- cut_note には従来通り加工メモとして記載（例: `"cut_note": "斜角60°カット・長辺2,077mm/短辺1,038mm"`）。
- 数量: 端部カットパネルは「切り捨て面積」をロス数量として quantities に計上（ロス率の目安もmdに掲載）。
- **地覆・壁高欄**: 延長は `length_mm / sin(θ)` ではなく橋軸沿いなら不変、支承線沿い部材なら実長換算。端部妻面が `90°-θ` の斜めカット。

> 層2の詳細（計算例・記入例）は実装完了後に md v5 として登録。**v4の「描けない形」から斜角を外すのは、層3〜5の実装・検証が済んでから**（それまで現場は申告運用を継続）。

## 3. 2D描画（平面図）

- `renderOverviewDiagram` の平面図を skew あり時は**平行四辺形**で描画（SVG polygon）。
- 支承線（A/A'）を斜めに、主桁は橋軸平行のまま（支承位置が桁ごとにずれて描かれる＝実態通り）。
- 食い違い量・斜角値・妻面実長を寸法線で明記。
- 端部カット範囲を斜線ハッチで可視化（「この範囲のパネルはカットになる」が一目で分かる）。

## 4. 3D描画

エンジンは四隅自由化済みなので追加プリミティブは不要。

- **床版本体**: BoxGeometry → 平面が平行四辺形のプリズム（8頂点 BufferGeometry、または上面/下面を `buildQuadGeometry` 相当の四隅で構成）。
- **妻側型枠 A/A'**: 実長 `W/sinθ` の面を、Y軸回転 `90°-θ` で支承線方向に配置。
- **B/B' 側型枠・主桁・底鋼板**: 従来通り（橋軸方向は無変化）。ただし主桁・底鋼板の端部は支承線で切られるため、見た目は「桁端が階段状にずれる」表現（各桁のBoxの長さを支承線までに調整）。
- **縦断勾配・むくりとの併用**: deckGrp 機構をそのまま使えるので干渉しない。

## 5. 検証（アプリ側チェック）

- `skew_angle_deg` があるとき、A/A'面の `width_mm` が `dimensions.width_mm / sin(θ)` と一致するか検証（±1mm）。不一致なら警告表示（「妻面長さが斜角と不整合」）。
- パネル幅合計チェック（既存）はそのまま適用可能（妻面実長に対して合計）。

## 6. 段階導入プラン

1. **S1**: スキーマ＋2D平面図の平行四辺形化＋検証（layer 1,3,5）— 効果が一番見える最小単位
2. **S2**: 3D対応（layer 4）
3. **S3**: md v5（layer 2: 計算ルール・記入例・「描ける形」への昇格）
4. **S4**: 地覆・壁高欄の斜め端部への横展開

各段階で関所（現物スクショ＋検算）を通す。

## 7. 要決定事項（くろたんレビューで確定したい）

- **Q1**: 斜角の定義（支承線∠橋軸、90=直橋）と `skew_direction` の既定はこれで現場感覚に合うか。
- **Q2**: パネルは「橋軸直角に流して端部カット」でよいか（支承線平行に流す現場流儀はあるか）。
- **Q3**: 端部カットのロス数量の計上方法（切り捨て面積をロス率で見るか、実カット寸法の積み上げか）。
- **Q4**: S1〜S4 の順序で現場的に困らないか（S3のmd更新を先にすべきか）。
