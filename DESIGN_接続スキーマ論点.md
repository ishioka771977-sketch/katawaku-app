# カタチ 接続スキーマ — 設計論点書（実装前・議論用）

> くろたん指令（2026-07-04）: 「コードを書き始める前に、接続スキーマの設計論点書
> （何と何を、どのキーで、どの粒度で結ぶか）を1本上げること。実装先行は後で必ず作り直しになる」
> 本書は**実装しない**。論点と選択肢を並べ、くろたん・ひでさんの判断を仰ぐ。

作成: 2026-07-04 / 状態: 論点出し（判断待ち）

---

## 0. 解きたい問題

補修工事の勘所は取り合い部の施工なのに、現状の型知は **1 JSON = 1 構造物**で、
「構造物Aが構造物Bにどの面で・どう接続するか」を書く場所がない。現運用は notes に文章で書くだけ。

対象の取り合い（現場頻度順・くろたん査定）:
1. 地覆 ↔ 床版（打継ぎ）
2. 壁高欄 ↔ 地覆（段差・目地一致）
3. 竪壁 ↔ 翼壁（鉛直打継ぎ・接続角度）
4. パラペット ↔ 竪壁（水平打継ぎ）

## 論点1: 「何と何を」結ぶか — 結合の単位

| 案 | 内容 | 利点 | 難点 |
|---|---|---|---|
| **A. 構造物×構造物** | 「地覆は床版に載る」レベル | 単純・書きやすい | 施工情報（どの面で・どの位置で）が入らず、結局notes頼み |
| **B. 面×面** | 「地覆のCB面下端が床版のB'面上端に接続」 | 型枠計画に直結（目地一致・段差が書ける） | 面IDが両JSONで安定している前提が要る |
| **C. エッジ×エッジ** | 面の辺単位で接続線を定義 | 幾何学的に最も正確 | 現状スキーマにエッジ概念がなく大改造 |

**私の仮見解**: B（面×面）。型枠アプリとしての価値（目地・打継ぎ・段差の整合確認）に直結する最小の粒度。
Cは3Dの理想論で、くろたんが書けない。Aは今のnotesと大差ない。

## 論点2: 「どのキーで」結ぶか — 参照の仕組み

前提: 現運用は 1 JSON = 1 構造物 = 1 会話。この運用は現場最優先設計（田中さんの負担ゼロ）の柱なので壊したくない。

| 案 | 内容 | 利点 | 難点 |
|---|---|---|---|
| **A. 単一JSON統合** | 1 JSONに複数構造物を入れ、内部IDで参照 | 参照が確実 | 1会話1構造物の運用が崩れる。JSONが巨大化しくろたんの出力が不安定になる |
| **B. project_id＋structure名で外部参照** | `connections: [{target: "宿野辺橋床版", ...}]` | 運用を変えない。project_master の名寄せ機構を再利用できる | 相手JSONが未登録だと解決できない（宙づり参照） |
| **C. 接続だけ独立JSON** | 「接続定義書」を第3のJSONとして書く | 構造物JSONを一切触らない | 管理対象が増える。どの会話で書くのか運用が不明確 |

**私の仮見解**: B。宙づり参照は「後から相手が登録されたら解決」でよい（project_master の
名寄せ・棚卸しと同じ思想）。片側からの宣言で成立し、双方向の整合はアプリが検証する。

## 論点3: 「どの粒度で」何を書くか — connection オブジェクトの中身

論点1=B・論点2=B を採るとして、最小フィールド案:

```json
"connections": [
  {
    "target_structure": "宿野辺橋 床版",        // project_master名寄せに乗せる相手名
    "self_face": "CB",                          // 自分側の面ID
    "target_face": "B'",                        // 相手側の面ID（分かる場合。省略可）
    "interface_type": "打継ぎ",                  // 打継ぎ／伸縮目地／段差／密着（一体打ち）
    "treatment": "チッピング＋レイタンス除去",
    "waterstop": false,
    "offset_note": "地覆前面は床版端から50mm内側",  // 位置関係は当面文章（→論点4）
    "joint_alignment": "床版の伸縮目地位置に地覆目地を一致させる"
  }
]
```

- **段階1では位置関係（offset）を数値化しない**。数値化は共有座標系（論点4）が決まらないと嘘になる。
  まず「接続の存在・種別・処理」を構造化するだけで、目地一致チェックと申し送りの明文化ができる。

## 論点4: 共有座標系 — 3Dマルチ構造シーンの前提

複数構造物を同一3D空間に置くには基準点の共有が要る。

| 案 | 内容 | コスト |
|---|---|---|
| A. 各構造物に `origin_offset` を持たせる（橋台A1の×××を原点、等） | 図面の座標系をそのまま使える | くろたんが図面から原点を読める必要 |
| B. connection に相対配置（自分の面X＝相手の面Yに一致）だけ書き、アプリが解く | くろたん負担ゼロ | 解決器の実装が重い。連鎖すると曖昧になる |
| C. 3D合成は当面やらない（接続情報は2D/帳票/検証にだけ使う） | ゼロ | 3Dの取り合い可視化は先送り |

**私の仮見解**: 段階1はC（3D合成しない）。connections の価値の大半は
「目地一致・打継ぎ処理の明文化と検証」であり、3D合成が無くても成立する。
3D合成はAで後日（origin_offset は省略可のオプション項目として予約だけしておく）。

## 論点5: 数量の重複計上防止

取り合い面は両構造物から「型枠不要（相手に密着）」になるケースがある（例: 地覆下端は床版天端に載るので底型枠不要）。
- 現状は各JSONのくろたん判断（bottom_form_required: false 等）で個別に処理している。
- connections 導入後は `interface_type: "密着"` から**アプリが「この面の型枠は不要のはず」と検証**できる
  （数量の二重計上・型枠の過剰計上の自動指摘）。段階1のキラー機能候補。

## 論点6: md・運用への影響

- くろたんの会話フロー: 従来通り1構造物ずつ。**会話の中で他構造物との取り合いが話題に出たら
  connections を書く**（相手が未登録でも書く）。
- 田中さんへの追加負担: ゼロ（訊かない。図面と会話から読み取れる範囲で書く）。
- 知見サマリー: 取り合いの申し送りは connections に構造化されるので、field_notes の「その他」から卒業。

## 提案する段階導入

1. **C1**: スキーマに `connections` 追加（論点1=B, 2=B, 3=最小案）＋md v5で記入ルール掲載
2. **C2**: アプリ検証（目地一致チェック・密着面の型枠要否チェック）＋全体確認図に接続一覧表示
3. **C3**: 3D合成（origin_offset 方式）— 需要データ（category: 3D非対応の集計）を見てから判断

## くろたんに決めてほしいこと

- **Q1**: 論点1〜4 の仮見解（B・B・最小案・段階1はC）で現場感覚に合うか。
- **Q2**: connection を書く**トリガー**の定義——「図面に取り合いが描かれていたら必ず」か「会話で話題に出たら」か。
- **Q3**: interface_type の語彙（打継ぎ／伸縮目地／段差／密着）はこの4つで足りるか。
- **Q4**: 論点5の検証（密着面の型枠要否指摘）は段階1に含めるべきか（実装コストは小さくない）。
